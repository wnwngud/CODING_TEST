import java.util.Scanner;
/*
백준 1789 - 수들의 합
 이 문제는 서로 다른 N개의 자연수의 합이 S라고 할 때, 자연수 N의 최댓값을 구하는 문제이다. 이 문제는 자연수의 합이 주어졌을 때 가장 많은
 가짓수의 자연수를 더해 S를 구성하는 것은 결국 제일 작은 값인 1에서부터 더해야 자연수의 가짓수를 최대로 할 수 있다. 따라서 1에서부터 시작해
 1씩 늘려가며 sum에 더하는데 만약 i를 더하기 전 sum + i가 S보다 같거나 적으면 sum에 i를 더하고 i++를 해준다. 그리고 sum + i가 S를 넘는다면
 반복문을 멈추고 i - 1를 출력한다. i가 아닌 i - 1를 출력하는 이유는 조건을 sum + i를 N과 비교해야하기 때문에 i++를 한 뒤 비교를한다.
 따라서 S이 200일 때 1~19까지의 합은 190인데 20을 더하면 210이기때문에 반복문을 멈춘다. 이 때 i값은 20이기 때문에 i - 1인 19가 정답이다.
 어떻게 더해야 S가 나오는지 굳이 구하지 않는 까닭은 만약 S의 값이 191이면 1~18까지 더하고 19를 20으로만 바꾸면 되기 때문에 1~20를 모두 더한 값인
 210만 되지 않는다면 자연수 N의 최대 가짓수는 19로 동일하기 때문이다.
 */
public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        long S = sc.nextLong(); // S(1 ≤ S ≤ 4,294,967,295)범위이므로 int사용 불가
        long sum = 0;
        long i = 1;
        while(sum + i <= S){
            sum += i;
            i++;
        }
        System.out.println(i - 1); //while문에서 i++하고 종료했으니 i-1개의 서로 다른 자연수를 더한 것이다.
        sc.close();
    }

}
